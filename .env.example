# Copy to .env.local for local dev (Vite exposes only VITE_* to the client)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Optional: backend API base URL for Axios (see README)
# VITE_API_BASE_URL=https://api.example.com
